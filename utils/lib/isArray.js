/**
 * Checks if a value is an array using {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/isArray Array.isArray})
 */
export function isArray(value) {
  return Array.isArray(value);
};
